spring:
  application:
    name: optimaxx-management-system
  profiles:
    default: dev
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
